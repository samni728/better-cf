# Changelog

## v1.3.0 - 2026-08-17

- 修复手动 `/16` 或 `/32` 仅作为普通提示、被大量历史网段稀释后可能完全没有候选的问题。
- SQLite 搜索记忆升级到 schema v3，保存用户输入中的种子 IP，并自动推导 IPv4 `/24` 或 IPv6 `/48` 深挖范围。
- 每轮先精确复测手动种子 IP；后续每批至少 40% 候选来自手动窄网段/父网段，且手动范围覆盖自动冷却。
- 任务冻结计划、设置面板和运行日志明确显示父网段、种子 IP、深挖范围及手动候选保底比例。
- 候选生成日志逐批显示手动范围与历史/全局候选数量，便于确认配置是否真实执行。

## v1.2.1 - 2026-08-17

- “本轮实际执行计划”新增搜索范围快照，明确列出每个协议族的手动优先父网段。
- 同时显示可复测 IP、窄网段、父网段、冷却 IP/网段及四类候选预算。
- 搜索计划在任务创建时冻结并持久化，后续修改设置不会改变历史任务展示。
- 运行日志明确输出实际使用的手动优先父网段。
- 设置页补充 CIDR 规范化说明：例如 `172.66.130.219/16` 会规范化为 `172.66.0.0/16`。

## v1.2.0 - 2026-08-16

- 将候选筛选明确拆成“实际机房与 RTT 发现 → 带宽候选 → HTTP/HTTPS 真连接”三个阶段。
- 把地区命中、带宽达标和带宽未达标分别写入 SQLite，不再只记最终真连接结果。
- 从官方 Cloudflare 地址池发现实际目标机房后，同时扩展 IPv4 `/16` 与 `/24`（IPv6 `/32` 与 `/48`），由宽到窄持续搜索。
- 近期带宽失败的具体 IP 冷却 6 小时，但保留其父/窄网段线索，避免重复失败且不会误杀整个潜力网段。
- 扫描器单次运行内不再重复测速同一个带宽失败 IP。
- 设置页搜索记忆增加地区命中和带宽阶段统计，运行日志明确显示两阶段准入流程。
- 保持带宽测速串行；快速响应/CF-RAY 继续使用现有高并发，避免 2 核、1GB VPS 因并行下载造成带宽争抢和假失败。

## v1.1.1 - 2026-08-16

- 将全部 DNS 配置能力与本轮实际执行计划分开显示。
- 明确列出本轮 IPv4 / IPv6 数量、真连接协议与端口组。
- 分别显示本轮 A / AAAA 记录数、实际参与目标和因协议未启用而跳过的目标。
- 将“确认 DNS 记录 / 目标”改为“Cloudflare 已核验记录（逐条）/ 已完成同步并复核的域名目标”，并补充计数解释。

## v1.1.0 - 2026-08-16

- 新增 SQLite 搜索记忆管理面板、成功/冷却网段和近 7 天采样覆盖率。
- 支持为单个搜索配置档案手动加入 IPv4 `/16` 或 IPv6 `/32` 优先父网段，并可独立清除档案。
- 根据候选来源近 7 天真连接成功率，自动调整历史精确 IP、窄网段、父网段和全局探索四类预算。
- 新增逐端口成功率、平均真连接延迟和测试出口/运营商标签分析。
- 自动识别 HTTP 至少一个端口可用、但 HTTPS 已测试且全部不可用的窄网段。
- Dashboard 的执行看板与目标概览改为上下排列。
- 页脚新增当前版本、GitHub 仓库地址和 Star 入口。

## v1.0.0 - 2026-08-15

- WebUI、Cloudflare 多凭据/多目标同步、地区筛选、真连接端口测试和基础 SQLite 搜索记忆形成首个完整闭环。
